# Production Environment Variables
# Copy these to your Vercel project settings under Environment Variables

# Backend API URL - Rozare backend on Railway
# Keep the trailing slash. The build also normalizes this defensively.
VITE_API_URL=https://rozare.up.railway.app/
