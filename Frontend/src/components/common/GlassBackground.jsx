import React, { useMemo } from 'react';

const GlassBackground = () => {
    // PERFORMANCE MODE: All animations disabled for 2018 Mac compatibility
    // Background orbs and bubbles completely removed to eliminate GPU lag
    return null;
};

export default React.memo(GlassBackground);
